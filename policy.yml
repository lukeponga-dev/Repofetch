on_http_request:
  - actions:
      - type: verify-webhook
        config:
          provider: github
          secret: ${GITHUB_WEBHOOK_SECRET}
      - type: forward
        config:
          addr: http://localhost:3000